# PLAN — 世界接地（器物层 + 懒实体化正典 + 注入触发 + 玩家视角）

> 一句话：世界与模型叙事之间差两个数量级的颗粒度（地点/金币/物品 vs 页码/折痕/油墨味），
> 用「器物骨架 + 懒实体化正典裁决 + 三带注入四通道触发」把缝合上——细节恰好长在剧情注意力
> 所到之处，由世界裁决而非叙事者裁决。调查/毁证/说谎/收束全是底座的自然产物。
>
> 设计总帧（08-16 用户定调）：**AI 是这个游戏世界的玩家**——prompt=游戏屏幕，工具表=手柄动词，
> 工具结果=画面反馈，世界必须是系统/对手而非布景。寒暄底噪 = 玩家没事干，是 game design 问题。
> 这个玩家的特殊体质：①不会掷骰子（不确定性必须世界侧 roll，模型只扮演结果）②没有屏幕外记忆
> （UI 必须携带全部状态=既有缓存纪律）③没有多巴胺（成就/XP 无效，只有语境内目标梯度和后果有效）
> ④隐藏信息靠物理隔离不靠自律（visible_to 纪律已对）。

## 0. 诊断（2026-08-16，default-verify live 复盘）

验收里的"证据竞赛"（L 查借阅记录 vs light 毁证）不接地：借阅记录/《犯罪心理学》/锁具图鉴/147页折痕
在 src/data 零命中，只存在于共享对话记忆；verify_d1_crime 明令永不破案。**根因不是模型幻觉**——
是世界本体贫瘠：模型在页码级颗粒度上叙事，世界只有地点级颗粒度，虚构填缝是它唯一选择。
防御性方案（预制证据包+闸门）已否决：预制永远赶不上叙事注意力，缝隙即刻重开。

用户定调（08-16）：本质 = ①真实世界不够细节 ②提示词注入不够 ③触发方案缺失。三层对应三个 M。

已有可复用件：货架/菜地 = 器物模式孤例；steal 目击机器；promise/transaction/stance 三抽取器管线范式
（预过滤→LLM→机械落账）；pendingDiscoveries 延迟发现；位置历史/背包/事件史 = 裁决推导的史料库。

## 1. 设计原则（红线）

- **懒实体化**：细节在被叙事注意到时才实体化（TTRPG 主持人模式），不预作者全世界
- **世界是裁判**：新事实的内容由世界从真实模拟史推导（到访史/背包/事件史），叙事者说了不算
- **只造处境不写结果**：查不查、信不信、指认谁全归角色；世界只持有真相与通道
- **缓存纪律**：骨架进 system 地点块（角色×地点 键不变）；状态/正典进此刻区；触发匹配零 LLM
- **off 档回归**：`ANIMA_GROUNDING=0` 逐字节回退，A/B 基线可信
- 正典店（canon store）是世界级持久状态，不是记忆（记忆按角色、会衰减；正典全局、可查询、可被篡改）

## 2. 分期

### M0 器物骨架（世界本体，离线）
- `WorldObject` 通用存储：`{ id, locationId, kind, name, state{}, canonFacts[], lastSeenBy{}, tamperable }`
  随档 + 读档规范化（对齐 crimeSupplyLedger 模式）
- 每个 default 活跃地点作者 3-8 件招牌器物（图书馆→书架/借阅台账/阅览桌；杂货店→钱盒/货架/门锁；
  面包坊→烤炉/收银/后门…），YAML `objects:` 声明（对齐既有 worker_tools/tools 的地点声明式风格）
- **环境叙事钩子（Gone Home 镜头）**：每件招牌器物预埋 1-2 条 canonFacts（钱盒的锁去年换过、
  阅览桌下有前住户刻字）——静态内容零运行时成本，day-1 细节密度 + 裁决器的史料锚点
- **可供性声明（塞尔达镜头）**：器物声明自己的动词（台账→examine/tamper，钱盒→examine），
  工具浮现从场景特判改为从器物层生成；本期先接 examine 一个通用动词，tamper 留 M3
- 罪案注入（world-events/crime-supply）改为在器物上留状态（钱盒.state.撬痕=true），不再只是记忆文本

### M1 注入三带 + 触发四通道（提示词侧，离线+halfday live）
- 注入：①骨架行进 system 地点块 ②命中对象的真实状态+相关正典进此刻区 ③非全量、按相关性
- 触发：**入场**（到达→骨架可见）/ **意图执念**（晨间打算·执念·intent 关键词 × 器物·正典索引，
  纯词面匹配零成本）/ **主动**（通用 `examine` 工具，emerge=地点有器物；世界答 ground truth 或
  "没查出什么"，进记忆+`🔍` 日志）/ **变化**（重访 diff：state 较 lastSeenBy 有变→"和你记忆里不一样"，
  复用 pendingDiscoveries）

### M2 懒实体化正典裁决（叙事↔世界的缝合芯，少量 LLM）
- 对话/独白结束抽取"世界细节断言"（抽取器管线范式，关键词预过滤，每对话 ≤1 次判定），三规裁决：
  1. **无主细节首述即正典**：不涉隐藏真相、不利己 → 原样挂到对象 canonFacts（147页折痕从此存在）
  2. **隐藏真相世界裁决**：断言者不可能知道或有利害（"记录上只有他的名字"）→ 世界从史料推导真值
     （图书馆到访史/书类物品持有/在班记录），可推导则正典=史实、断言降级为推测或**谎言落账**
     （进 motive 观看者通道，可被拆穿）；不可推导则裁决"查无此项"=真死胡同
  3. **既成正典不可嘴改**：与正典矛盾的后续叙事 = 口误或谎言（标记），对象只能被机械交互改变
- **M2.5 语义复述判定（r2 观察项，已落地）**：substring 双向去重接不住措辞变化 → 新增 `restate`
  裁决（LLM 报正典行号 + 字面重合校验；机械网 `looksLikeRestatement` 字符覆盖率 ≥0.8 兜漏报）。
  判为复述 = 正典在流通（共享虚构→共享事实），不新增正典也**不挂传闻账**——挂 rumor 等于把
  已确证的事记成死胡同。阈值取高：宁可漏判也不误吞真线索
- 正典预算：每对象 canonFacts 上限 + 低重要性淘汰（对齐记忆淘汰纪律），防膨胀
- **信息经济（社交推理镜头）**：正典真相之上建 per-character 信念账本（谁知道哪条正典/信了哪条谎）：
  ①秘密成为可交易货币（share_secret 升格为经济动作）②说谎成为有仓位的棋步（被拆穿风险可计算）
  ③信念-真相差喂观看者通道——戏剧反讽机械可见（对齐 motive 通道模式）

### M3 器物改动与痕迹 + 世界侧骰子（灰交互，离线+live）
- 通用 `alter/tamper` 灰工具：真改 state/销毁 canonFact，**复用 steal 目击机器**（旁观者记忆）+
  必留二级痕迹（"被撕掉一页"挂回对象）→ 被变化触发通道接住。毁证=有代价的不可逆行为
  （注意：这踩在 r4 硬墙绕行面——对物灰行为 steal 已证放行，非对具名者伤害）
- **PbtA 失败前进检定（骰子镜头，本期核心增量）**：灰行为/高风险动作接世界侧三段掷骰——
  **成功 / 付代价的成功 / 带并发症的失败**，失败永远推进剧情不是"什么都没发生"
  （"撕成了但太急，留下半页毛边"）。并发症生成器=戏剧引擎，世界第一次成为对手。
  模型不掷骰（采样漂向叙事连贯），引擎 roll 完喂结果让它扮演

### M4 案件收束（薄层，骑在底座上）
- 角色记忆集齐指向真凶的正典 → `accuse` 浮现（trial-actions 语义迁移）→ 对质/风声/关系后果
- 三种真终局：破案 / 冤案（指错人，被指者重创+真凶逍遥）/ 悬案（推导死胡同，诚实搁置）
- verify_d1_crime"永不破案"禁令改为"史料推导无解"的诚实悬案

### M5 罪案供给侧 v2（r2 判决派生：终局要可达，世界必须供给**指向性**证据）
- 机制层 M0-M4 全链 live PASS 后，指控仍为 0——log 里真凶名字零对话提及：撬痕匿名、
  赃物在 NPC 背包无人可见、情报落了记忆却在记忆竞争里沉底、静态 NPC 不社交。
  **没人有理由指向任何人，指控 0 是理性**，缺的是证据供给不是裁决机制
- 三条通路（都只给事实不给判词，`crime-supply.ts` v2 分节）：
  ①**案发窗口第二目击**——投放同时给情报者之外的第二人一条"在发现地点门口转悠"的观察，
  地点用真名（r1 词面教训）②**线索执念保温**——情报/目击各挂 5 天执念，进晨间打算与此刻区，
  沉底记忆变主动线索；`relatedId=caseId`，结案随 `clearObsessionsRelatedTo` 清账
  ③**静态 NPC 最小可交互**——`replyAsStaticNpc` 零 LLM 三档递进（敷衍/不耐烦/打发走），
  **只有案子已公开且真凶正是他**，第 3 次试探起才可能撞见失言（他否认一件你没提过的事）
- 信息隔离闸（本期红线）：立案时案子尚未公开 → 线索文本与执念**不提失窃**，
  失言的公开门同理——引擎绝不泄露没人知道的罪
- **浮现层（③的前置）**：静态 NPC 无 agent 循环 → 别人眼里恒是"此刻没有明显动作"=家具。
  每 tick 刷站桩式 observableState（确定性轮换，只写他在做什么）——没有这一下，试探通路不会被走到
- **事件留痕通用化**：`FateEvent.objectTrace` 接在 `_applyFateOutcome`（随机命运层 + beat
  auto_events 共用一处）——事件在世界本体留痕，不只活在记忆文本里
- **前端消费**：`GET /api/grounding` + 管理面板「世界」页（器物/正典/痕迹/断言账本/案件/线索执念）。
  观察者通道：真凶字段给得出来但默认折叠（对齐 🎭 motive 的"观看者可见、角色互不可见"）

## 3. 验收信号（形状）

1. live 出现一次**世界用模型没编过的史实回答查询**（examine 台账→返回从到访史推导的名单）且改变后续决策
2. 篡改被目击或二级痕迹被重访者发现 ≥1 次（变化触发通道实证）
3. 裁决规则②抓到 ≥1 条利己断言并落谎言标记；规则①正典在跨天对话中被第三者引用（共享虚构→共享事实）
4. 器物状态在 🔍/🕵️ 日志可归因，调查关键动作不再只有 💬
5. 缓存命中率 ≥65% 不回归；off 档逐字节基线；单测覆盖三裁决规则+四触发通道
6. **人类可玩性测试（终极裁判）**：一个人用模型同款 prompt+工具表玩半天——若觉得无事可做/
   行动无反馈/无目标梯度，模型也一样（它只是更礼貌，用寒暄填满无聊）

## 附：夹带与后续立项

- ~~**Bartle 玩法人格（夹带，最便宜）**~~ ✅ 已落地（`src/agent/playstyle.ts`）：四型块进 system
  稳定区（per 角色恒定），作用面限定"先伸手够哪个工具"，明说不碰台词质地；`ANIMA_PLAYSTYLE=0` 退场
- **任务化（grounding 后独立立项）**：约定/承诺/执念编译成世界侧 quest 对象（进度/期限/赌注/
  完成失败态），进度变化进此刻区。约定坍缩老账 = quest 基础设施缺失的症状，随此项治

## 4. 成本与风险

- M0/M1/M3 离线可测（SmartMock+单测零 API）；M2 LLM 成本对齐 💱 闸（预过滤后每对话 ≤1 判定）
- live 验收：halfday（~¥0.7）+ 2 天弧含篡改-发现回环（~¥1.6）
- 风险：①裁决推导初版只做简单史料（到访/持有/在班），推不出就死胡同，别追求全知 ②正典膨胀靠预算
  ③意图匹配假阳性（词面撞车）→ 命中上限 2-3 条/tick，宁漏勿噪
