# DESIGN — 决裂层（赌注搬进关系之内）

> **状态：提案，待用户拍板。** 2026-08-28 起草，动机来自 drama-verify live1 判决 +
> 诊断第三组遗留（「赌注永远在关系之外」）。实现前先过一轮对抗评审（DESIGN-revival 同规格）。
>
> ⚠️ **前置依赖（live2 长弧发现，同日追加）**：和解清账目前**不吃机械核验**——
> live2 实证真嗣一句「15金我已经还了」（钱分文未动）就让 reconcile 把 tier 2 阶梯
> 整个抹平。这个洞不堵，升档链永远可以被一句好话拆除，本设计的判据（tier 3 + 爽约）
> **永远够不着**，决裂层盖在沙上。修法见 live2 判决「下一枪①」：可核验 kind 的账
> 没动 → reconcile 不得清该 kind 的拒绝账。**先落那一枪，再动本设计。**

## 一、病灶

诊断原话：**关系会 decay 回中性，永远碎不掉。** 具体到机制：

- 升档链走到 tier 3 的措辞是「这条路走到头了」——但走到头之后**什么都不发生**。
  摊牌的终局还是"又一场对话"，平局病在弧线层复发：场景层每场戏有胜负了（S1），
  弧线层"我们之间"永远回得到起点
- 世界已有三种不可逆的「输」：失业（失去已有的）、追求到期（永远错过想要的）、
  信念击穿（自我认知被改写）。**唯独关系——这个世界最大的资产类别——没有终局形式**
- live1 实证的反面：明日香收尾说「别等我哪天忘了，你可就省了这笔钱了」——
  威胁背后没有任何机械后果在等着。她说的"忘了"就是 decay 的日常，狼真的不会来

## 二、机制形状（三条腿，全部复用既有模式）

### 1. 决裂是不可逆状态，不是数值

`relationships` 条目加 `ruptured: { tick, reason }`（随档 + normalize，grudge 同款载体）。
一旦置位**永不清除**（可逆的决裂是狼来了；失业同款纪律：本期不做和好，先把「碎」做完整）。

置位那刻的落账（全部走既有管线，不新开）：
- LTM 双方 imp9（被决裂方：「你把TA的话用完了」）+ 编年史 💔 imp9
- 信念计数器加第七指标 `ruptures`（作者可写「没有人会留下来」类击穿判据）
- **追求联动**：pursuit metric 是 relationship 且 target 是对方 → **立即永久失败**
  （决裂 = 那条路的物理封死；这是决裂的第一颗真牙，零新代码——pursuit 层已有失败态）

### 2. 进入判据：机械、有向、零 LLM（红线同追求/信念层）

两条路，都从摊牌链的尽头长出来（「tier 3 没有去开灰工具的门」——这就是那扇门）：

- **心死（自动）**：`tier == 3` 时再落一笔 `brokenPromise` 爽约 → 被骗方对爽约方决裂。
  只认爽约不认被拒——被回绝四次是钉子，答应了又放空四次是心死。
  预警纪律齐全：tier 1/2/3 的所求行措辞就是三级警告，摊牌是最后通牒，
  与失业的「先警告后辞退」同构（不可逆发生前，加害方有过 ≥4 次选择）
- **了断（主动）**：`sever_ties` 工具，`tier ≥ 3` 浮现（我方账本视角）。
  把话说死是合法社交行为不是作恶，不撞行为层硬墙；选不选归角色
  （预期 killer/achiever 型先用；steal 28 次零采纳的教训在此不适用——
  了断没有道德税，它是自尊行为）

### 3. 决裂后的世界（真后果，不是换措辞）

- **回暖通道物理关闭**：registerTalk 加分对这对停用、idleDecay 不再拉回中性、
  level 封顶在决裂时值（可以更坏，不能更好）
- **摊牌兜底对这对永久跳过**——「这条路走到头了」终于是真的
- **所求层**：不再生成 bond/aspiration/hostile 向所求（事情以"再无来往"的方式结了）；
  **债务所求保留**（钱是钱，决裂不豁免欠账——"人可以断，账不能断"自带张力）
- **prompt 帧**：关系行换成决裂措辞（「你们之间已经断了」），进 system 稳定区
  （⚠️ 与信念击穿同款缓存代价：置位那刻砸一次前缀，一局最多几次）
- 对话不禁止（同镇避不开），欠条照常机械核验——**决裂不是剧终，是新常态**：
  "前朋友 + 未清的账 + 编年史都记得"本身就是第二幕的燃料

## 三、刻意不做（non-goals，防散架）

1. **和好线**：不可逆就是不可逆。做了回头路，前面所有警告贬值为噪音
2. **复仇线**：决裂后压力边定格，不自动升级成 grudge/加害——那是另一层的题
3. **剪辑**（诊断另一条）：均质流问题与本层正交，别搭车
4. **LLM 判「他们决裂了吗」**：绝不。判据只认账本

## 四、验收形状

- 单测：判据（tier3+爽约→裂 / tier2+爽约→不裂）、不可逆（置位后 easeRefusal/得手
  不解除）、追求联动失败、回暖关闭、off 档 `ANIMA_RUPTURE=0` 逐字节退场、随档往返
- seeds 已支持预热（`refusals: count:3` 即 tier 3 进场）→ drama-verify 加一对
  标定在决裂判据下方一格，短跑可验"那一下"；长弧验决裂后的新常态质感
- 判读三数：决裂次数（预期一局 0-1 次——比信念击穿更稀有才对）、
  决裂后这对的互动形态（有没有第二幕）、sever_ties 采纳率

## 五、预算

实现 ~1 个会话（状态载体+判据+后果接线+单测），live 验收搭 drama-verify 弧（≈¥2）。

风险最大的一处已勘察（2026-08-28，只读调研）：正向漂移有两个口——`registerTalk`
（对话回暖，grudge 冻结先例就在里面）和散布在 simulation/agent-loop 的十来处
`modify(+N)`（和解+3/兑现+2/赴约+3/还钱+3…）。**闸必须装在 RelationshipManager
内部**（registerTalk / modify / idleDecay 三处，同一文件单一所有者）：ruptured 对
正向 delta 钳 0、负向照常、decay 跳过——绝不去改十几个调用点（漏一处 = "决裂了
还在回暖"的假状态，voice 两条腿的教训）。
