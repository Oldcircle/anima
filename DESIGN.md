# Anima — 详细架构设计

## 第一性原理

> **每个角色（包括玩家）是一个独立的 Agent。**
> **Agent 通过工具（tool use）与世界交互，自主决策，没有特殊机制。**
> **对话、社交、工作、生活——所有行为都是工具调用，没有例外。**

灵感来源：OpenClaw 把人类行为打包成工具放在系统提示词里，让 AI 自己决定要做什么。Anima 做同样的事——只不过工具不是"读文件/发消息/搜索网页"，而是"吃饭/串门/聊天/喝酒/钓鱼"。

### 什么是 Agent

```
Agent = 人格（system prompt）
      + 感知（当前世界状态注入 user prompt）
      + 记忆（短期 + 长期，注入 prompt）
      + 行为空间（tools）
      + 一次 LLM 调用 → 决定做什么
```

**没有对话系统，没有特殊的社交机制。** `talk` 就是一个普通的工具，和 `eat`、`sleep` 没有本质区别。角色调用 `talk` 发一句话给目标，目标在自己的下一个 tick 看到这句话，自主决定是否回应。对话跨 tick 自然展开，世界不暂停。

### 玩家 = Agent

玩家和 AI 角色使用完全相同的工具集。唯一区别：
- AI 角色的决策来自 LLM
- 玩家的决策来自前端输入

```
玩家点击"咖啡馆" = 调用 go_to("cafe")
玩家输入"嘿 Alice" = 调用 talk("alice", "嘿 Alice！")
```

玩家有位置、需求、金币、关系——和 NPC 完全对称。

---

## 一、核心系统

### 1.1 时间系统（Tick Engine）

```
现实时间 × 速率倍数 = 游戏时间
```

- **基础比率**：现实 1 分钟 = 游戏 1 小时
- **Tick 粒度**：每个 tick = 游戏 15 分钟 = 现实 15 秒（1x 速率下）
- **速度控制**：暂停 / 0.25x / 0.5x / 1x / 2x / 5x / 10x
- **一个游戏日** = 96 个 tick = 现实 24 分钟（1x 速率下）

每个 tick，每个 Agent 独立做一次决策。

### 1.2 世界状态（World State）

- **地点**：住宅区、商业区（花店/面包店/杂货店/咖啡馆/酒吧）、公共区（广场/图书馆）、自然区（海边/森林/农田）
- **天气**：按季节概率随机，影响 Agent 决策
- **季节/节日**：春夏秋冬各 28 天，固定节日注入所有 Agent 的 prompt
- **经济**：角色有金币，工作赚钱，消费扣钱

### 1.3 事件总线 + 消息信箱

所有行为产生事件，广播到事件总线。

**消息信箱（Inbox）**：每个角色有一个消息队列。当 A 对 B 调用 `talk`，消息写入 B 的信箱。B 在下一个 tick 的 prompt 中看到这条消息，自主决定如何回应。

```typescript
interface Inbox {
  messages: Array<{
    fromId: string;
    fromName: string;
    content: string;
    tick: number;
  }>;
}
```

这是对话发生的唯一机制——没有 Conversation 对象，没有阻塞循环。

---

## 二、角色系统

### 2.1 角色卡（Character Card）

每个角色一个 YAML 文件，定义人格、背景、日程、初始关系。

### 2.2 需求系统

| 需求 | 衰减 | 满足方式 |
|------|------|---------|
| 饥饿 | -2/tick | eat +50 |
| 精力 | -1/tick | sleep +全满 |
| 社交 | -1/tick | talk +15, gossip +20 |
| 快乐 | 不衰减 | hobby +15, 不愉快 -10~30 |
| 卫生 | -0.5/tick | wash +全满 |

需求值影响 Agent 决策：低需求在 prompt 中被强调。

### 2.3 关系系统

关系值 -100 到 100，通过互动自然演化。`talk` 每次 +3，`give_gift` +5，`argue` -10。类型自动推算（stranger → acquaintance → friend → close_friend → best_friend / rival）。

---

## 三、Agent 系统

### 3.1 行为工具（全部平等）

每个工具就是角色可以做的一件事。**`talk` 不特殊。**

**日常类：** eat, sleep, wash, cook
**工作类：** work, fish, craft, trade
**社交类：** go_to, talk, give_gift, gossip, argue, comfort, flirt
**休闲类：** read, explore, drink, hobby
**内省类：** reflect, plan_day, recall

### 3.2 `talk` 工具的行为

```
talk(target, message)
  → 消息写入 target 的信箱
  → 发送者获得少量社交值
  → 广播事件到事件总线（witnesses 可传播八卦）
  → 结束。不阻塞，不等待回复。
```

对方在自己的下一个 tick 看到信箱里的消息。可以 `talk` 回复，也可以无视。

这意味着：
- 对话自然跨多个 tick（每 tick 一句），像真人发消息
- 角色可以"已读不回"——这是人格的表达
- 同一地点多人可以互相发消息——群聊自然发生
- 世界不暂停，其他角色继续生活

### 3.3 Agent 决策循环

每个 tick 对每个角色执行：

```
1. 构建 prompt:
   - 系统提示词（人格 + 行为规则）
   - 当前状态（时间、位置、天气、需求值）
   - 感知（附近的人 + 他们的关系）
   - 信箱（有人对你说了什么）
   - 近期记忆（最近的行为和观察）
   - 日程提示
   - 可用工具列表

2. LLM 返回:
   - 内心独白（思考过程）
   - 工具调用（行为）

3. 执行工具:
   - 更新世界状态
   - 如果是 talk → 写入目标信箱
   - 广播事件
   - 更新需求和关系

4. 存储记忆
```

### 3.4 Token 优化

- **惰性决策**：多 tick 行为跳过中间 tick
- **日程兜底**：独处 + 需求正常 → 按日程执行，不调 LLM
- **社交驱动**：社交值低或信箱有消息 → 强制调 LLM
- **夜间跳过**：23:00-05:00 自动睡觉
- **模型分层**：日常决策用便宜模型

### 3.5 玩家集成

```
前端操作 → WebSocket → 转化为工具调用 → 和 AI 角色进入同一个执行管道

点击地点 → go_to(location)
输入文字 → talk(target, message)
点击物品 → give_gift(target, item) / eat(location, food)
```

玩家有自己的 CharacterState（位置、需求、金币、关系），前端显示和 AI 角色相同的状态面板。

---

## 四、记忆系统

### 4.1 短期记忆
- 滑动窗口，最近 30 条
- 包含：自己的行为、收到的消息、观察到的事件、八卦

### 4.2 信箱（Inbox）
- 别人对你说的话
- 每 tick 消费并注入 prompt 的 "## 有人对你说" 段落
- 已读消息移入短期记忆

### 4.3 长期记忆（待实现）
- SQLite + 向量搜索
- 反思机制生成高层洞察
- 跨天记忆检索

---

## 五、前端

### 5.1 布局

```
┌─────────────────────────────────────────────────┐
│  时间 | 天气 | [⏸ ½ 1x 2x 5x] | 连接状态       │
├──────────────────────┬──────────────────────────┤
│                      │  角色面板 / 全局面板      │
│   小镇地图           │  - 需求条                │
│   (地点卡片+角色)    │  - 关系                  │
│                      │  - 信箱（收到的消息）     │
│                      │  - 最近行为              │
├──────────────────────┤                          │
│  事件流（底部）       │  玩家操作区              │
│  行为/消息/八卦/事件  │  [输入框] [发送]         │
└──────────────────────┴──────────────────────────┘
```

### 5.2 玩家操作

- 点击地点 → 移动
- 选中角色 → 查看状态 / 发送消息
- 输入框 → talk
- 玩家状态面板（需求、金币、关系）

---

## 六、测试体系

- **单元测试**：时间、需求、关系、事件总线、信箱
- **集成测试**：Agent 循环 + mock LLM、信箱消息传递
- **Live 测试**：真实 LLM 调用，验证 Agent 行为
- **模拟测试**：5 角色跑 1-7 天，观察涌现行为

关键验收标准：
- 两个角色在同一地点时，通过信箱自然产生跨 tick 对话
- 角色可以"已读不回"
- 3 人在广场时可以多人互相说话
- 玩家的 talk 和 AI 的 talk 走完全相同的管道
