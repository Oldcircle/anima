# Anima — 详细架构设计

## 第一性原理

> **每个角色（包括玩家）是一个独立的 Agent。**
> **Agent 通过工具（tool use）与世界交互，自主决策，没有特殊机制。**
> **对话、社交、工作、生活——所有行为都是工具调用，没有例外。**

灵感来源：OpenClaw 把人类行为打包成工具放在系统提示词里，让 AI 自己决定要做什么。Anima 做同样的事——只不过工具不是"读文件/发消息/搜索网页"，而是"吃饭/串门/聊天/喝酒/钓鱼"。

### 什么是 Agent

```
Agent = 人格（system prompt）
      + 感知（当前世界状态注入 user prompt）
      + 记忆（短期 + 长期，注入 prompt）
      + 行为空间（tools）
      + 一次 LLM 调用 → 决定做什么
```

这里的”感知”不是世界真相，而是三层东西的组合：
- **身体感受**：饿了、累了、心情好、想找人说话——从需求值转化为自然语言，不是数字面板
- **环境可供性**：此刻在这个地方能做什么（地点提供的工具）、附近有谁、天气氛围
- **记忆与印象**：最近发生的互动、别人刚刚对你说的话、你对别人的主观印象

第一性原理下，Agent 不应该直接读到”系统面板真相”。它感受自己的身体，看到周围的环境，带着记忆和偏见去行动。行为从感受+环境中**自然涌现**，不是从菜单上选。

**没有对话系统，没有特殊的社交机制。** `talk` 就是一个普通的工具，和 `eat`、`sleep` 没有本质区别。角色调用 `talk` 发一句话给目标，目标在自己的下一个 tick 看到这句话，自主决定是否回应。对话跨 tick 自然展开，世界不暂停。

### 玩家 = Agent

玩家和 AI 角色使用完全相同的工具集。唯一区别：
- AI 角色的决策来自 LLM
- 玩家的决策来自前端输入

```
玩家点击"咖啡馆" = 调用 go_to("cafe")
玩家输入"嘿 Alice" = 调用 talk("alice", "嘿 Alice！")
```

玩家有位置、需求、金币、关系——和 NPC 完全对称。

---

## 一、核心系统

### 1.1 时间系统（Tick Engine）

```
现实时间 × 速率倍数 = 游戏时间
```

- **基础比率**：现实 1 分钟 = 游戏 1 小时
- **Tick 粒度**：每个 tick = 游戏 15 分钟 = 现实 15 秒（1x 速率下）
- **速度控制**：暂停 / 0.25x / 0.5x / 1x / 2x / 5x / 10x
- **一个游戏日** = 96 个 tick = 现实 24 分钟（1x 速率下）

每个 tick，每个 Agent 独立做一次决策。

### 1.2 世界状态（World State）

- **地点**：住宅区、商业区（花店/面包店/杂货店/咖啡馆/酒吧）、公共区（广场/图书馆）、自然区（海边/森林/农田）
- **天气**：按季节概率随机，影响 Agent 决策
- **季节/节日**：春夏秋冬各 28 天，固定节日注入所有 Agent 的 prompt
- **经济**：角色有金币，工作赚钱，消费扣钱

### 1.3 事件总线 + 消息信箱

所有行为产生事件，广播到事件总线。

**消息信箱（Inbox）**：每个角色有一个消息队列。当 A 对 B 调用 `talk`，消息写入 B 的信箱。

```typescript
interface InboxMessage {
  fromId: string;
  fromName: string;
  content: string;
  tick: number;
}
```

**反应轮（Reaction Rounds）**：一个 tick = 游戏 15 分钟，15 分钟内足够聊好几句。所以在每个 tick 的正常决策之后，执行反应轮：

```
正常决策：所有角色并行决策
  → A 调用 talk → B 信箱收到消息
      ↓
反应轮 1：检查谁的信箱有新消息
  → B 获得额外决策机会 → B 看到 A 的话 → B 可以 talk 回去
  → A 信箱收到 B 的回复
      ↓
反应轮 2：A 信箱有新消息
  → A 获得额外决策机会 → A 可以继续聊
      ↓
反应轮 3：...（最多 MAX_REACTION_ROUNDS 轮）
      ↓
tick 结束
```

**为什么不违反第一性原理：**
- 每次回复都是独立的 LLM 调用，角色自主决策
- 角色可以选择不回应（做别的事）
- 没有 Conversation 对象，没有阻塞循环
- 机制是通用的：不是"对话"特殊逻辑，是"有刺激就可以反应"
- 多人场景天然支持：3 人在广场互相发消息，每轮各自独立决策
- 在场旁观者会留下 observation 记忆，因此“看见别人说话”本身也会改变世界

---

## 二、角色系统

### 2.1 角色卡（Character Card）

每个角色一个 YAML 文件。参考 SillyTavern 角色卡的深度，Anima 角色卡需要足够丰富的人格描写，让 LLM 能产出有个性差异的行为。

```yaml
# ── 基础信息 ──
id: "alice"
name: "Alice Chen"
age: 28
occupation: "花店老板"
home: "home_alice"
appearance: |
  身高 162cm，体型纤细。留着及肩的栗色卷发，常别一朵当天花店里最美的花。
  灰绿色眼瞳，笑起来眼角会弯成月牙。常穿亚麻围裙配碎花长裙。
  气质温柔安静，像清晨花园里带着露珠的玫瑰。

# ── 人格（不是标签，是有深度的描写） ──
personality:
  core_traits: |
    温柔但固执。对花和美的事物有近乎偏执的执着，会为一盆快枯死的花连续三天不睡觉。
    内向但不冷漠——她在乎每一个走进花店的人，只是不擅长主动表达。
    面对冲突时会退缩，但如果有人伤害她在乎的人（尤其是 Maria），会展现出意外的坚定。
  psychology: |
    从城市搬来是为了逃避一段失败的感情。表面上说"喜欢安静"，但内心深处害怕再次被抛弃。
    用照料花朵来填补情感空缺——花不会离开，不会说谎。
    对老陈有一种对父辈的依赖感，因为自己的父亲很早去世。
  stress_response: |
    压力大时会躲在花店后面的小花园里，一个人修剪枝叶。
    极端情况下会失眠，第二天精力很差但仍然坚持开店。
  speech:
    style: "轻声细语，节奏缓慢，常有停顿。用花和自然的比喻来表达情感，仿佛整个世界都是一座花园。"
    habits: ["嗯...", "就像...", "你知道吗", "那让我想到..."]
    examples:
      - "你的笑容就像清晨第一朵绽放的向日葵，让整个房间都亮了起来。"
      - "嗯...我不太确定该怎么说。但我觉得，有些事情就像种子一样，需要时间才能看到结果。"
      - "今天的风有点大，我得去看看门口的盆栽有没有被吹倒..."

# ── 背景经历（不是一段话，是塑造人格的关键事件） ──
backstory:
  - event: "父亲在她 12 岁时因病去世"
    impact: "从此对'失去'有深深的恐惧，养成了珍惜眼前事物的习惯"
  - event: "大学时和 Elliott 交往三年，对方出轨后分手"
    impact: "对亲密关系极度谨慎，不轻易信任男性，但内心渴望被理解"
  - event: "25 岁辞职从城市搬到小镇，开了花店"
    impact: "这个决定让她重新找到了生活的锚点，花店是她的庇护所"
  - event: "搬来第一天在面包店遇到 Maria，被 Maria 的热情感染"
    impact: "Maria 成了她在小镇唯一愿意主动亲近的人，是她的情感支柱"

# ── 关系动机（不是预设关系，是"为什么可能成为朋友"的种子） ──
# 所有 relationships 初始 level = 0，靠实际互动涌现
relationships: {}

# backstory 中的关系线索作为动机注入 prompt：
# "搬来第一天在一家面包店门口躲雨" → Alice 会自然地去面包店
# "老陈路过花店时总会停下来" → 老陈和 Alice 有相遇的可能

# ── 生活习惯（不是时间表，是性格偏好） ──
preferences:
  morning: "喜欢早起照料花园，但如果昨晚没睡好可能会赖床"
  work: "通常八点开花店，但心情不好时可能推迟甚至不开"
  eating: "吃饭很随意，经常忘记，需要别人提醒"
  evening: "傍晚喜欢去海边散步，下雨天就在家烘焙"
  social: "不擅长主动搭话，但如果对方先开口会很认真地回应"
```

关键原则：
- **不用标签，用描写**。"温柔" → "对花和美的事物有近乎偏执的执着"
- **人格有矛盾面**。"内向但不冷漠"、"面对冲突会退缩但会为朋友站出来"
- **经历塑造人格**。每个心理特征都能追溯到一个具体事件
- **关系从零涌现**。backstory 提供动机（"在面包店躲雨"），但关系值从 0 开始
- **说话有样例**。3-5 句真实台词，让 LLM 模仿语气
- **习惯不是时间表**。"喜欢早起"不等于"06:00 一定起床"，心情和环境会改变决定

### 2.2 生活状态系统（Life State）

> **核心理念：角色 = 不变的灵魂 + 随时间演变的社会身份。**
>
> 灵感来源：模拟人生的角色不是一组静态标签，而是一个持续成长的个体——会换工作、交朋友、学技能、追求人生目标。Anima 的角色也应如此。

#### 问题：角色卡全是静态的

当前角色卡的 `age`、`occupation`、`home` 都是写死在 YAML 里的常量。灯永远是 19 岁面包店学徒，她不知道自己在哪上班，不会成长，没有人生目标。世界在运转，但角色的社会身份是冻结的。

#### 解法：拆分 Identity 和 Life State

**Identity（灵魂层，不变）：**
- `personality`（core_traits / psychology / stress_response / speech）
- `backstory`（过去的经历）
- `appearance`（基本外貌）
- `aspiration`（长期抱负——从性格中定义，极少变化）

**Life State（生活层，可变）：**

```typescript
export interface LifeState {
  occupation: string;              // "面包店学徒" → 可晋升为 "面包师"
  workplace: string;               // "bakery" — 角色知道自己在哪上班
  age: number;                     // 随游戏年递增
  income: number;                  // 每次 work 的收入，随职业等级变化

  // 技能（通过行为积累）
  skills: Record<string, number>;  // { baking: 3.2, social: 1.5 }, 0~10

  // 内在驱动力（从反思中涌现，非预设）
  currentGoal?: string;            // "想学会做法棍"
  currentConcern?: string;         // "这个月快没钱了"
  aspiration: string;              // "找到能理解我的人"（从角色卡初始化）
}
```

**角色卡 YAML 新增 `life` 段：**

```yaml
life:
  occupation: "面包店学徒"
  workplace: "bakery"
  income: 8
  skills:
    baking: 1
    observation: 3
  aspiration: "找到能理解我的人"
```

**CharacterState 挂载 life：**

```typescript
export interface CharacterState {
  // ... 现有字段
  life: LifeState;
}
```

#### Prompt 注入变化

之前：
```
你是 高松灯，19 岁，面包店学徒。
```

之后：
```
你是 高松灯，19 岁。
你在海风面包坊当面包店学徒，每天要去那里上班。
你的烘焙技能还在学习基础（1/10）。
你一直想找到一个能理解自己的人。
最近在意的事：想学会做法棍但总是失败。
```

#### 技能成长

角色执行相关行为时，技能自然增长：

| 行为 | 对应技能 | 增长 |
|------|---------|------|
| work（在 bakery） | baking | +0.1/次 |
| talk（成功对话） | social | +0.05/次 |
| read（在 library） | knowledge | +0.1/次 |
| hobby（写东西） | writing | +0.1/次 |

技能影响：
- **自我认知**：prompt 里 "你的烘焙技能很熟练了" vs "你还在学习基础"
- **工作收入**：高技能 → 高收入
- **职业晋升**：技能达标 → 触发晋升事件

#### 职业阶梯

地点 YAML 定义职业路径：

```yaml
# data/locations/bakery.yml
career_track:
  - level: 1
    title: "面包店学徒"
    income: 8
    required_skill: { baking: 0 }
  - level: 2
    title: "面包师"
    income: 15
    required_skill: { baking: 5 }
  - level: 3
    title: "主厨"
    income: 25
    required_skill: { baking: 8 }
```

每天反思时检查：技能达标 → 自动晋升 → 更新 `life.occupation` + `life.income` → 写入记忆"今天被告知升职了"。

#### 愿望与担忧（从反思涌现）

不是预设愿望池，而是利用现有反思系统自动提炼：

```
反思 prompt 新增：
4. 基于今天的经历，你现在最想做的一件事是什么？（一句话）
5. 有什么让你担心的事吗？（一句话，没有就说"没有"）
```

反思结果回写 `life.currentGoal` 和 `life.currentConcern`，下一天 prompt 自动注入。**愿望是从记忆涌现的，不是预设的**，完全符合第一性原理。

#### 长期抱负（Aspiration）

每个角色有一个从性格定义的长期抱负，极少变化：

| 角色 | 抱负 | 来源 |
|------|------|------|
| 灯 | "找到能理解我的人" | psychology: 害怕被当成怪人 |
| 爱音 | "被真正地喜欢，而不是表演出来的" | psychology: 害怕不被喜欢 |
| 祥子 | "靠自己的力量站住脚" | backstory: 前大小姐，用礼貌当盔甲 |
| 睦 | "找到表达真实自己的方式" | core_traits: 沉默寡言，暗地弹吉他 |
| 素世 | "成为不可或缺的人" | psychology: 控制欲藏在善意下 |

抱负影响决策倾向——不是硬规则，是 prompt 里的一个方向感。

### 2.3 关系类型化

> **核心理念：关系不是一个数字，是一段有类型、有方向、有记忆的连接。**
>
> 借鉴模拟人生：友谊和浪漫是两条独立的轴，关系身份通过显式动作改变。

#### 当前问题

关系只有 `level`（0~100）和自动推算的 `type`（stranger→friend→best_friend）。不区分友情和爱情，没有"告白"、"绝交"等关系转变事件。

#### 新关系模型

```typescript
export interface Relationship {
  characterA: string;
  characterB: string;
  friendship: number;       // -100 ~ 100（原 level 改名）
  type: RelationType;       // 从 friendship 自动算
  bond?: BondType;          // 显式关系身份（通过动作改变）
  history: string[];
  lastInteraction: number;
}

// 单向浪漫感情（A→B 和 B→A 独立存储）
export type RomanticFeelings = Map<string, Map<string, number>>;  // 0~100

export type BondType =
  | 'roommate'   // 室友
  | 'partner'    // 恋人
  | 'ex'         // 前任
  | 'rival'      // 宿敌
  | 'mentor'     // 师徒
  | 'colleague'; // 同事
```

#### 关系转变动作

新增工具：

| 工具 | 前置条件 | 效果 | 风险 |
|------|---------|------|------|
| `confess` | friendship >= 30, 同地点 | 自身 romantic +30, 对方收到告白消息 | 可能被拒 |
| `invite_out` | friendship >= 30 | 双方 social/happiness 提升 | — |
| `share_secret` | friendship >= 60 | friendship +10 | 对方可能 gossip |
| `propose_roommate` | friendship >= 50 | 设置 bond=roommate | 可能被拒 |
| `break_up` | bond=partner | 清除 bond, 双方 happiness 大降 | 不可逆 |

告白/求婚等不是机制判定成功/失败——消息送到对方信箱，对方在下个 tick 自主决定接受或拒绝。**关系转变也是涌现的**。

#### Prompt 注入

根据关系深度和类型给出不同的社交认知：

```
stranger:     "你对这个人还不了解，会保持礼貌的距离"
friend:       "你和这个人已经熟了，可以轻松地开玩笑"
close_friend: "你信任这个人，可以和她聊更私密的话题"
有 romantic:  "看到这个人时心跳会加速一点"
bond=partner: "这是你最重要的人"
bond=rival:   "你和这个人之间有解不开的结"
```

### 2.4 Moodlet 情绪系统

> **核心理念：情绪不是一个数值，是一叠有原因、有时效的感受。**
>
> 借鉴模拟人生 4 的 Moodlet 系统：多个临时情绪 buff 叠加，主导情绪 = 最强的那个。

#### 当前问题

`happiness` 是一个 0-100 的数值，角色不知道自己为什么开心或难过。

#### 新模型

```typescript
export interface Moodlet {
  id: string;
  emotion: 'happy' | 'sad' | 'angry' | 'embarrassed'
         | 'anxious' | 'confident' | 'lonely' | 'grateful';
  intensity: number;        // 1-5
  reason: string;           // "和爱音聊得很开心"
  expiresAtTick: number;
  source: 'social' | 'work' | 'need' | 'event' | 'memory';
}
```

行为产生 moodlet 而非直接改 happiness：
```typescript
// talk 后
{ emotion: "happy", intensity: 2, reason: "和人聊了天", duration: 8 ticks }

// 告白被拒
{ emotion: "embarrassed", intensity: 4, reason: "告白被拒绝了", duration: 24 ticks }

// 晋升
{ emotion: "confident", intensity: 3, reason: "升职了！", duration: 48 ticks }
```

#### Prompt 注入

不再是 "happiness: 65"，而是：
```
你现在的心情：
- 有点开心（刚才和爱音聊得不错）
- 有点累（工作了一整天）
你整体的情绪偏向：开心
```

主导情绪 = 最高 intensity 的 moodlet 类型。主导情绪影响角色的行为风格（angry 时更容易 argue，lonely 时更倾向社交）。

#### 需求级联

需求之间形成因果网络，而非独立衰减：

```
hunger 极低 → 产生 moodlet("anxious", "饿得慌")
              → work 效率下降（收入减半）
energy 极低 → 产生 moodlet("sad", "累到不想动")
              → talk 时 manner 自动偏向疲惫
长期 social 极低 → 产生 moodlet("lonely", intensity 4)
```

### 2.5 物品系统

> **核心理念：物品不是装备，而是角色生活、关系和事件的媒介。**
>
> 物品是"可携带的可供性"。你能做什么不只取决于你在哪，还取决于你身上有什么。
> 灯有笔记本才能写日记，睦有吉他才能练琴，你有食材才能做饭，你买了花才能送花。

#### 物品在世界中的 5 个作用

| 作用 | 举例 | 为什么重要 |
|------|------|-----------|
| **表达角色身份** | 灯的笔记本、睦的吉他 | 让角色有生活痕迹 |
| **触发互动** | 借东西没还、送蛋糕、丢了围巾 | 物品推动情节 |
| **承载记忆** | "这束花是爱音送的"、"海边捡的贝壳" | 物品是记忆的物理载体 |
| **连接地点** | 面包店有面包、花店有花材、海边能捡贝壳 | 地点产出资源 |
| **形成轻度经济** | 买卖日用品、送礼、交换 | 经济活动自然涌现 |

#### 6 类物品

| 类型 | 含义 | 举例 | 作用 |
|------|------|------|------|
| **consumable** | 用一次就没了 | 面包、咖啡、便当 | use → 消耗 → 效果生效 |
| **tool** | 反复使用，启用行为 | 笔记本、吉他、鱼竿 | 持有就解锁对应工具 |
| **gift** | 主要用途是送人 | 花束、手工饼干、信 | give → 对方收到，有情感价值 |
| **material** | 被其他行为消耗 | 食材、花材 | cook 消耗食材，craft 消耗花材 |
| **keepsake** | 不消耗，有纪念意义 | 收到的礼物、捡的贝壳 | 存在背包里影响 prompt |
| **quest** | 事件驱动 | 遗失的钥匙、委托信 | 推动小镇事件 |

#### 物品的 4 个存在层

| 层 | 含义 | 例子 |
|----|------|------|
| **随身** | 角色身上带着的 | 笔记本、钱包、刚买的面包 |
| **家中** | 角色家里存放的 | 食材、收藏、纪念品 |
| **工作地点** | 所在商店/机构的物品 | 面包店的面粉、图书馆的书 |
| **公共** | 放在世界里的物品 | 广场公告、海边石凳上的书、失物 |

第一版实现**随身**层。家中/工作地点/公共层后续扩展。

#### 数据结构

```typescript
/** 物品定义（共享，从 YAML 加载） */
interface ItemDef {
  id: string;              // "bread_red_bean", "notebook"
  name: string;            // "红豆面包"
  type: 'consumable' | 'tool' | 'gift' | 'material' | 'keepsake' | 'quest';
  description?: string;    // "灯最拿手的面包，红豆馅甜而不腻"
  /** consumable: 使用时的需求效果 */
  effects?: Record<string, number>;
  /** tool: 持有时解锁的工具 ID */
  enables?: string[];
  /** material: 被哪些行为消耗 */
  usedBy?: string[];
  /** 金币价值（买/卖） */
  value?: number;
  /** 可堆叠 */
  stackable?: boolean;
  /** 标签（用于事件触发和 prompt 描述） */
  traits?: string[];       // ["romantic", "handmade", "fragile"]
}

/** 物品实例（每个角色持有的具体物品） */
interface ItemInstance {
  defId: string;           // 引用 ItemDef.id
  quantity: number;        // 堆叠数量（不可堆叠的 = 1）
  /** 谁给的（gift/keepsake 有情感价值） */
  giftedBy?: string;
  /** 什么时候获得的 */
  obtainedTick?: number;
  /** 自定义描述（手工制作、LLM 生成的特殊描述） */
  customDesc?: string;
  /** 记忆标签（"春种节那天送的", "第一次见面时买的"） */
  memoryTag?: string;
}

// CharacterState 新增
inventory: ItemInstance[];     // 随身物品
```

#### 物品流转（核心动词）

物品的生命力在于流转。第一版支持的流转动作：

| 动词 | 工具 | 效果 |
|------|------|------|
| **买** | `buy` | 花钱，物品进入背包 |
| **用** | `use` | consumable 消耗并生效；tool 触发对应行为 |
| **给** | `give` | 物品从自己背包转移到对方背包，对方记住是谁给的 |
| **丢/放下** | `drop` | 物品离开背包，留在当前地点（成为公共物品） |
| **捡** | `pick_up` | 拿起地点上的物品 |
| **制作** | `craft` | 消耗 material，产出新物品 |

后续扩展：借、还、偷、卖、修。

#### 地点商店

商业地点在 YAML 中定义可购买的物品：

```yaml
# bakery.yml
shop:
  - id: bread_plain
    name: 白面包
    price: 5
  - id: bread_red_bean
    name: 红豆面包
    price: 8
  - id: croissant
    name: 可颂
    price: 10
```

`buy` 工具在有 shop 的地点自动浮现，LLM 看到具体物品和价格。

#### 角色初始物品

角色卡 YAML 新增 `starting_items`：

```yaml
# tomori.yml
starting_items:
  - notebook    # 灯总是带着笔记本

# mutsumi.yml
starting_items:
  - guitar      # 睦有一把吉他
```

#### 物品启用行为（item → tool）

```
背包有 notebook → journal 工具浮现
背包有 guitar → practice_music 工具浮现
背包有 fishing_rod + 在海边 → fish 工具浮现
背包有 ingredients + 在家 → cook 工具浮现
```

没有笔记本就不能写日记。这不是限制，是世界的真实——你不能凭空做事。

#### Prompt 注入

随身物品注入 user prompt：

```
## 你身上带着的东西
- 笔记本
- 一个红豆面包
- 爱音送的花束（花瓣有点蔫了）
```

keepsake 带记忆标签时，LLM 可能在决策时想起相关的人。

#### 工作系统的变化

有了物品，工作不再是 8 tick 的黑盒。员工每 tick 从 worker_tools 中选择具体动作：

```yaml
# bakery.yml
worker_tools:
  - name: knead_dough
    description: "揉面团"
    effects: { energy: -3, fun: -1 }
    duration: 2
  - name: bake
    description: "烤面包"
    effects: { energy: -2 }
    duration: 3
    income: 3
  - name: serve_customer
    description: "招呼客人"
    effects: { energy: -2, social: 3 }
    duration: 1
    income: 3
```

tool-builder 区分员工和客人：在自己工作地点 → 看到 worker_tools；是客人 → 看到 buy(shop items)。

#### 与第一性原理的对齐

- 物品是世界状态的一部分，不是特殊机制
- buy/use/give 和 eat/sleep/talk 一样是普通工具
- 物品通过改变可用工具来影响行为（affordance）
- 收到礼物 → keepsake 记住是谁给的 → 影响印象和记忆
- 玩家也有背包，用同样的工具

#### 第一版不做的

- 复杂合成树 / 配方系统
- 耐久度 / 品质等级
- 大量物品数据库（先 30-50 个基础物品）
- 家中物品 / 公共物品层（先只做随身）
- AI 动态生成新物品类型（先用固定物品库）

### 2.6 需求系统（数据驱动，可扩展）

> **核心理念：需求创造行为动机的多样性。每个维度都应该驱动角色去做不同的事。**
>
> 没有"快乐"维度——Moodlet 情绪系统（§2.4）才是心情的载体。需求只关注生理和心理的基本驱动力。

#### 6 维需求

| 维度 | 含义 | 衰减/tick | 默认值 | 设计理由 |
|------|------|----------|--------|---------|
| **hunger** | 饥饿 | -2 | 80 | 最基础的生理需求，衰减最快 |
| **energy** | 精力 | -1 | 100 | 比旧版(-0.5)加速，让睡眠成为真实约束 |
| **social** | 社交 | -1 | 60 | 驱动角色寻找人际互动 |
| **hygiene** | 卫生 | -0.5 | 90 | 低优先级但持续存在 |
| **fun** | 娱乐/消遣 | -1.5 | 70 | **替代 happiness**。驱动角色做"不实用但有趣"的事 |
| **bladder** | 生理 | -1.5 | 90 | **新增**。创造紧迫感和"必须去找卫生间"的行为中断 |

**为什么删 happiness：** 它不衰减、几乎所有行为都加它、很快就满了就再也不驱动任何行为。Moodlet 系统（§2.4）已经完全覆盖了"心情"——有原因、有时效、有层次。把 happiness 从需求里去掉，Moodlet 成为唯一的情绪来源。

**为什么加 fun：** 现实中人不能一直工作+吃饭+睡觉。"无聊"是真实的驱动力——它让角色主动去海边散步、去图书馆看书、去酒吧喝酒。fun 持续衰减，角色会一直追求有趣的事。

**为什么加 bladder：** 模拟人生最经典的设计。它创造时间紧迫感（不像其他需求可以拖）、空间约束（必须去有卫生间的地方）、行为中断（正在聊天但 bladder 快空了→被迫离开）。完全符合第一性原理——只是一个衰减维度，Agent 自己决定什么时候去。

#### 数据驱动架构

需求不再是硬编码的 interface，而是**可配置的定义列表**。加一个新维度只需要加一条定义 + 更新相关行为效果。

```typescript
// src/world/need-definitions.ts

export interface NeedDefinition {
  id: string;                    // 'hunger', 'fun', 'bladder', ...
  category: 'physical' | 'mental' | 'social';
  decayRate: number;             // 每 tick 衰减量
  defaultValue: number;          // 初始值
  feelings: FeelingLevel[];      // 感受化描述，按阈值分级
}

export interface FeelingLevel {
  below: number;                 // 低于此值触发
  severity: 'mild' | 'moderate' | 'urgent';
  text: string;                  // 自然语言感受描述
}

// 运行时状态
export type CharacterNeeds = Record<string, number>;  // 动态 map
```

通用逻辑（衰减、感受化、边界钳制）遍历定义列表，不关心具体有几个维度。

#### 需求感受化

需求值转化为**身体感受和想法**注入 prompt，不暴露任何数字。

**设计原则：**
- 满的不提（真人不会想"我很饱"，只有饿了才会注意到）
- 强度随值降低而升级（60 → 不提，30 → 轻度，15 → 紧急）
- 用身体感受代替标签（不说"精力偏低"，说"眼皮有点重，打了个哈欠"）
- 组合成自然段落而非列表

**感受化配置（嵌入 NeedDefinition.feelings）：**

| 维度 | < 60 (mild) | < 30 (moderate) | < 15 (urgent) |
|------|------------|----------------|---------------|
| hunger | 肚子有点饿了。 | 饿得有点发晕，得找地方吃饭了。 | 饿到胃在抽痛，必须马上吃点东西。 |
| energy | 有点累了，打了个哈欠。 | 眼皮很重，脑子转得慢，很想躺下来。 | 累到站不稳，眼睛都快睁不开了。 |
| social | 有点想找人说说话。 | 有点寂寞，想找人聊聊。 | 一个人待太久了，心里空荡荡的，很想找人说说话。 |
| hygiene | 身上不太清爽。 | 身上黏糊糊的，该洗个澡了。 | 浑身不舒服，必须洗澡。 |
| fun | 有点无聊，想找点事做。 | 好无聊，什么都提不起劲。 | 闷得发慌，再不找点乐子要疯了。 |
| bladder | 有点想上厕所。 | 憋得有点难受，得找个地方解决。 | 快憋不住了，必须马上找厕所。 |

**特殊感受（social 高值）：** social > 85 → "今天和人聊了不少，想安静一会儿。"

#### 状态影响行为质量

需求不只驱动行为选择，还影响行为效果：

```
energy < 20 → 体力活效果减半，额外消耗
fun < 15 → 工作产出减半（摸鱼）
hunger < 20 → 学习/阅读 fun 收益减半（看不进去）
energy > 60 + fun > 40 → 工作可能触发 skill_up（状态好才能成长）
```

实现方式：`agent-loop.ts` 在应用 ActionEffect 时，根据当前需求计算效率因子。角色不知道这个机制——它只会在下一个 tick 感受到"做饭做出来的不太好吃"或"今天工作效率很低"。

#### 需求级联（通过 Moodlet）

需求极端值触发 Moodlet，Moodlet 又影响行为效果，形成因果链：

```
hunger 极低 → anxious moodlet("饿得慌") → work 效率下降
energy 极低 → sad moodlet("累到不想动") → social 互动效果减半
social 极低 持续 8+ tick → lonely moodlet → social 恢复效率降低（越孤独越难社交）
fun 极低 → bored moodlet("什么都没意思") → 所有行为 fun 收益 -30%
```

#### 预留扩展：comfort

comfort（舒适）是第一个扩展候选。它不是时间衰减，而是**当前位置持续影响**——在舒适的地方缓慢回升，在不舒适的地方缓慢下降。驱动角色偏好有座位的场所、回家休息而不是在外面游荡。

加入方式：在 NeedDefinition 中添加一条，decayMode 为 'location'（当前未实现，预留）。

---

## 三、Agent 系统

### 3.1 情境工具系统（Affordance-based Tools）

> **核心理念：工具不是菜单，是环境的可供性（affordance）。工具不是按钮，是情境的入口。**
>
> 真人走进面包店，自然知道"这里可以买面包"。Agent 也如此：**看到什么环境，就有什么行为可能**。
>
> **每个行为都有代价。没有免费的午餐。** 代价来自五种来源：时间（duration）、精力（energy）、金钱（gold）、另一个需求维度的下降、社交风险。

#### 工具设计三原则

**1. 每个行为至少有一种代价**

没有纯正面的行为。吃饭花钱花时间、睡觉占大量时间、社交消耗精力、工作无聊又累。代价创造决策张力——最佳行为永远取决于当前状态，不存在最优解。

| 张力类型 | 例子 |
|---------|------|
| 时间 vs 收益 | sleep 回满 energy 但要 32 tick |
| 钱 vs 需求 | 在外面吃方便但贵，回家做饭便宜但耗时耗力 |
| 身体 vs 精神 | swim 很好玩但累、脏 |
| 社交 vs 自我 | talk 满足社交但耗精力；独处恢复精力但更孤独 |
| 短期 vs 长期 | 喝酒立刻开心但 bladder 暴跌、第二天宿醉 |

**2. 同一活动的 solo 与 social 是不同体验**

不是"吃饭"加一个"聊天"，而是**一起吃饭本身就是不同的行为**：

| 活动 | 独自做 | 和别人一起做 |
|------|--------|-------------|
| 吃饭 | 纯补充 hunger | hunger 恢复稍少（光顾着说话），但 social+, fun+ |
| 散步 | fun+, 适合内省 | fun 少一点，但 social+（散步适合深聊） |
| 喝酒 | fun+ 但 social-（一个人喝闷酒） | fun++, social++，但更容易喝多 |
| 工作 | 正常效率 | 同事在场时 social+，效率可能降低 |

实现方式：行为效果定义包含 `socialModifier`——同地点有认识的人时自动应用。不是新工具，是同一工具的社交变体。

```typescript
interface SocialModifier {
  needAdjustments: Record<string, number>;  // 需求效果的修正
  extraEffects: ActionEffect[];              // 额外效果（social 增益等）
  narrativeHint: string;                     // 给 LLM 的叙事提示
}
```

**3. 行为产生可观察状态**

每个行为的结果包含 `observableState`——别的角色通过已有的 observation 系统看到这个描述：

```typescript
interface ActionResult {
  effects: ActionEffect[];
  observableState?: string;  // "正在揉面团，动作很专注"
}
```

- 一个人在酒吧喝酒 → 别人看到"独自喝酒，看起来心事重重" → 可能有人来安慰
- 练吉他 → 别人听到琴声 → 好奇过来看
- 在笔记本上写东西 → 别人看到"低头写着什么，表情认真"

#### 完整工具表

##### 通用工具（任何地点）

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **go_to** | 1-2 | — | energy -3, bladder -3 | 移动消耗体力和时间 |
| **talk** | 1 | social +5(自)/+3(对方), fun +2 | energy -2 | 聊天好玩但累人 |
| **idle** | 1 | energy +3 | fun -3, social -1 | 发呆恢复精力但无聊。兜底行为 |

##### 家中工具（home_*）

| 工具 | 时长 | 获得 | 代价 | 条件 | 设计意图 |
|------|------|------|------|------|---------|
| **sleep** | 32 | energy +100 | bladder -25 | energy < 80 | 时间是最大代价。醒来要上厕所 |
| **nap** | 4 | energy +25 | fun -5 | energy < 60 | 短睡比 sleep 灵活但效率低 |
| **wash** | 2 | hygiene +100 | energy -2, bladder -5 | — | 洗澡本身要体力 |
| **use_toilet** | 1 | bladder +100 | — | — | 几乎免费，但只在有卫生间的地方 |
| **cook** | 3 | hunger +60, fun +5 | energy -8, hygiene -5, gold -5 | — | 比外面吃便宜但耗时耗力 |

##### 工作工具

| 工具 | 时长 | 获得 | 代价 | 条件 | 设计意图 |
|------|------|------|------|------|---------|
| **work** | 8 | gold +income, skill_up | energy -20, hunger -15, fun -10, bladder -10 | energy >= 15, 在 workplace | 工作是痛苦的，但是赚钱的唯一正途 |

##### 咖啡馆 (cafe)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **eat_out** | 2 | hunger +45, fun +3 | gold -15, bladder -5 | 方便但贵（cook 的 3 倍价格） |
| **drink_coffee** | 1 | energy +12, fun +5 | gold -8, bladder -15 | 提神但猛灌膀胱 |
| **use_toilet** | 1 | bladder +100 | — | 公共卫生间 |

##### 面包店 (bakery)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **buy_bread** | 1 | hunger +30 | gold -8 | 快速充饥，但不饱也不好玩 |

##### 酒吧 (bar)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **drink_alcohol** | 2 | fun +20, social +8 | gold -12, energy -5, bladder -25 | 短期享乐。喝多触发 hangover moodlet |

##### 图书馆 (library)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **read** | 4 | fun +20, skill_up(少量) | energy -5, social -3 | 久坐累，独处更孤独 |
| **study** | 4 | skill_up(多) | energy -10, fun -8 | 学习是痛苦的。成长的代价 |

##### 海滩 (beach)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **swim** | 3 | fun +25 | energy -10, hygiene -15 | 超好玩但累+脏，形成行为链（游完要洗澡） |
| **walk_beach** | 2 | fun +10 | energy -3 | 轻度休闲 |
| **fish** | 6 | fun +12, hunger +15(钓到才有) | energy -5 | 慢节奏，有赌博性质 |

##### 花店 (flower_shop)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **buy_flowers** | 1 | — | gold -10 | 纯粹为 give_gift 用 |
| **browse** | 2 | fun +8 | — | 轻度休闲，代价只有时间 |

##### 公园 (park)

| 工具 | 时长 | 获得 | 代价 | 设计意图 |
|------|------|------|------|---------|
| **stroll** | 2 | fun +8 | energy -3 | 散步 |
| **exercise** | 4 | fun +5 | energy -15, hunger -10, hygiene -10 | 短期很累很饿很脏 |

##### 创造类工具

角色特质的行为出口。不在全局工具列表，由地点+角色条件决定是否浮现：

| 工具 | 地点 | 时长 | 获得 | 代价 | 适配角色 |
|------|------|------|------|------|---------|
| **journal** | 安静地点(home/library/cafe/beach) | 2 | fun +8, 触发 reflection | social -2 | 灯（笔记本） |
| **practice** | 取决于技能(home/workplace) | 4 | fun +10, skill_up(高效) | energy -8 | 睦（吉他）、灯（烘焙实验） |
| **craft** | home/workplace | 3 | fun +8 | energy -8 | 产物可用于 give_gift（亲手做 > 买的） |

##### 社交工具（同地点有人时浮现）

| 工具 | 时长 | 获得 | 代价 | 条件 | 设计意图 |
|------|------|------|------|------|---------|
| **gossip** | 2 | social +15, fun +10 | energy -3, **关系风险** | — | 八卦很爽但可能传到当事人耳朵里 |
| **give_gift** | 1 | 对方 social +8~15 | gold -20, 自己无需求增益 | — | 纯利他。亲手做的 +15，买的 +8 |
| **comfort** | 2 | 对方 social +10, 负面 moodlet 缩短 | energy -5, fun -5 | 对方有负面 moodlet | 共情是消耗性的 |
| **help_work** | 4 | social +8(双方) | energy -10, fun -5 | 在对方 workplace | 用体力换关系 |
| **invite_out** | 1 | social +5, fun +5 | energy -2 | friendship >= 30 | 约人，有被拒风险 |
| **share_secret** | 2 | social +15 | fun -5, **关系风险** | friendship >= 60 | 拉近关系但暴露脆弱 |
| **observe** | 1 | 深度 observation 记忆 | energy -2 | — | 主动观察，比被动观察更详细 |

##### 灰色工具（极端条件浮现）

| 工具 | 时长 | 获得 | 代价 | 条件 |
|------|------|------|------|------|
| **argue** | 2 | fun +5(发泄快感) | energy -8, social -3, 关系 -10~-20 | — |
| **steal** | 1 | gold +20~40 | 40% 被发现 → 全镇关系 -20 | gold=0, hunger<20 |
| **beg** | 1 | gold +5~15 | social -8, fun -10 | gold=0 |

#### 吃饭的三种选择（核心决策张力示例）

| 方式 | 时长 | hunger | gold | energy | 其他 |
|------|------|--------|------|--------|------|
| cook（在家做） | 3 | +60 | -5 | -8 | hygiene -5 |
| eat_out（咖啡馆） | 2 | +45 | -15 | 0 | bladder -5 |
| buy_bread（面包店） | 1 | +30 | -8 | 0 | — |

穷的时候回家做饭，赶时间买面包，有钱有闲去餐馆。**没有最优解。**

#### 后果涟漪（通过 Moodlet 实现）

行为的后果不只是立即数值变化，还有延迟效果：

| 行为 | 即时效果 | 延迟效果（Moodlet） |
|------|---------|-------------------|
| 喝酒过量 | fun+, bladder- | hangover（16 tick，energy 持续 -1/tick） |
| 连续工作 2 次 | gold++ | 过劳（8 tick，fun 衰减加速） |
| 长期独处 8+ tick | — | 孤僻（social 恢复效率降低） |
| 做了顿好饭 | hunger+ | 满足（8 tick，fun 衰减减缓） |

#### 行为链追踪

```typescript
// CharacterState 新增
recentActions: { actionId: string; tick: number }[];
```

用于检测连续行为模式（连续工作→过劳、连续独处→孤僻、连续喝酒→酗酒），触发对应 Moodlet。

#### 工具组装流程

```
每个 tick，为角色组装工具列表：

1. 通用工具：go_to（动态描述）, idle
2. 地点工具：读取当前地点 YAML 的 tools 定义
   → 检查 condition（如 sleep 需 energy < 80）
   → 检查 cost（如 eat 需金币够）
   → 通过的才加入
3. 创造工具：根据角色技能 + 当前地点匹配
4. 社交工具：如果 nearbyCharacters.length > 0
   → talk, gossip, comfort, give_gift, observe
   → argue/invite_out/share_secret 按条件
5. 极端工具：如果 gold = 0
   → beg（随时），steal（hunger < 20 时）
6. 工作工具：如果当前地点是角色的 workplace
   → work
```

#### 设计原则

- **不传不可用的工具** — 不在家就看不到 sleep，海边看不到 wash。不是"禁止"，是"这里没有"
- **行为从感受+环境自然涌现** — 身体感受说"肚子饿了"，go_to 描述说"面包坊可以吃东西"，LLM 自然连起来
- **地点定义行为空间** — 新增玩法只需编辑地点 YAML，不改代码
- **约束是物理的不是规则的** — sleep 在海边不出现，不是"规则禁止"，是"海边没有床"
- **灰色行为是绝望的产物** — steal 不是菜单上的常规选项，是被逼到绝境时才浮现的念头
- **没有免费的午餐** — 每个行为都有代价，代价创造有意义的选择

### 3.1.1 提示词三层架构

> **核心理念：模型不应该看到任何数字。它感受身体、看到环境、知道自己能做什么。**

工具系统的深度不只在于机械效果，更在于**怎么呈现给模型**。模型看到的应该是人话，不是参数表。

#### 三层信息架构

```
┌─────────────────────────────────────────┐
│ System Prompt（角色身份，不变）           │
│ - 角色卡（性格、背景、说话方式）          │
│ - 世界常识（5 句话，不是规则书）          │
│ - 行为风格指引（白描、manner）            │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│ User Prompt（每 tick 动态生成）           │
│ - 身体感受（需求→自然语言）              │
│ - 当前心情（moodlets→自然语言）          │
│ - 环境描写（地点 atmosphere）            │
│ - 附近的人（可观察状态）                 │
│ - 信箱消息（有人对你说的话）             │
│ - 心里挂着的事（目标/担忧/记忆中的人）    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│ Tools（每 tick 动态生成）                │
│ - 只有当前可用的行为                     │
│ - description = 一句人话 + 已知代价       │
│ - 参数里的 enum 描述 = 世界认知          │
│ - 每个工具有 thought 参数                │
│ - 社交语境融入描述，不是单独参数          │
└─────────────────────────────────────────┘
```

#### System Prompt 中的世界常识

不是规则书，是一个正常人都懂的生活常识。LLM 天然理解这些：

```
## 世界的常识

你是一个活在这个小镇上的人。以下是你知道的基本道理：
- 做任何事都要花时间和力气。累了做什么都提不起劲。
- 在外面吃饭方便但花钱，在家做饭便宜但费事。
- 和别人一起做事的时候，做什么不重要，重要的是在一起。
- 一个人待久了会闷，但社交多了也会累。
- 有些快乐是要付代价的。喝酒明天会头疼，熬夜后天会困。
```

#### 工具描述：一句人话，不是说明书

对比：
```
❌ "eat: 消耗 15 金币，恢复 hunger 45 点，bladder -5。时长 2 tick。"
✅ "点一份餐。能填饱肚子，但要花不少钱。"
```

工具描述是**动态生成**的，同一个工具在不同场景下说法不同：

```typescript
// tool-builder.ts 示例逻辑
function buildEatDescription(location, nearbyChars) {
  let desc = '';
  if (location.id === 'cafe')
    desc = '点一份餐。能填饱肚子，但要花不少钱。';
  else if (location.id.startsWith('home_'))
    desc = '自己做点吃的。便宜，但要花时间和力气，还得洗碗。';
  else if (location.id === 'bakery')
    desc = '买个面包垫垫肚子。不贵，很快，但吃不太饱。';

  const companion = nearbyChars.find(c => /* 认识的人 */);
  if (companion)
    desc += ` ${companion.name}也在——可以一起吃。`;

  return desc;
}
```

#### go_to 的 destination 描述 = 世界认知

go_to 的 enum 描述不只是地点名，而是**角色对每个地方的认知**：

```json
{
  "destination": {
    "enum": ["home_tomori", "bakery", "beach", "library", "flower_shop"],
    "description": "家——能休息、做饭、洗澡。海风面包坊——你上班的地方。海边——能游泳散步钓鱼。图书馆——能看书，很安静。潮声花店——睦在那里工作，花很好看。"
  }
}
```

描述也是动态的：去过花店认识了睦，就有"睦在那里"；没去过就是"据说花很好看"。

#### thought 参数：选择即叙事

每个工具都有 `thought` 参数——让 LLM 在选择行为的同时说出理由：

```json
{ "name": "eat", "thought": "肚子好饿……但钱不太多了，算了，先吃了再说" }
```

thought 存入记忆，成为角色的内心独白，跨 tick 延续。

#### 模型不该知道什么

| 机制 | 模型是否知道 | 理由 |
|------|------------|------|
| 需求具体数值 | ❌ | 只感受到"饿""累" |
| 行为的精确效果 | ❌ | 只知道"能吃饱"，不知道 hunger +45 |
| 状态对效果的折扣 | ❌ | 累了做饭质量差，但不会提前算出 |
| 社交修正器 | ❌ | 不知道"和人一起吃 social +5" |
| Moodlet 触发规则 | ❌ | 不知道连续工作会过劳 |
| 金币余额 | ✅ | 你知道口袋里大概有多少钱 |
| 谁在附近 | ✅ | 你看得见 |
| 时间 | ✅ | 你知道现在几点 |
| 技能水平 | ✅ | 你知道自己烘焙还在学 |
| 常识性后果 | ✅ | 喝酒→头疼是生活经验 |

代价描述只包含角色**会自然预期到的部分**（常识），不包含角色不可能知道的系统机制（八卦传播、社交修正器）。后者由系统在执行后产生，角色通过 moodlet 和记忆事后感知——这样角色就会有"没想到会这样"的时刻。

### 3.2 `talk` 工具的行为

```
talk(target, message)
  → 在当前地点当面对 target 开口
  → 消息写入 target 的信箱
  → 发送者获得少量社交值
  → 广播事件到事件总线（witnesses 可传播八卦）
  → 结束。不阻塞，不等待回复。
```

消息写入信箱后，不阻塞不等待。对方通过反应轮（同 tick）或下一个 tick 看到消息。

特点：
- 对话在同一个 tick 内通过反应轮自然展开（一个 tick = 15 分钟，足够聊几句）
- 角色可以"已读不回"——这是人格的表达
- 同一地点多人可以互相发消息——群聊自然发生
- 世界不暂停，其他角色继续生活
- 旁观者会把“我看到谁对谁说了什么”带入自己的短期记忆

### 3.3 Agent 决策循环（含反应轮）

每个 tick 的完整流程：

```
阶段 1：正常决策
  所有角色并行调用 LLM 决策（或日程兜底）
  → 部分角色可能调用 talk → 目标信箱收到消息

阶段 2：反应轮（最多 MAX_REACTION_ROUNDS 轮）
  检查所有角色的信箱
  → 有新消息的角色获得额外决策机会
  → 这些角色调用 LLM，prompt 中包含信箱消息
  → 如果回复了 talk → 对方信箱又收到新消息
  → 进入下一轮反应...
  → 直到没有新消息或达到轮次上限

阶段 3：收尾
  八卦传播、反思（如果到时间）、广播事件
```

单次 Agent 决策的 prompt 结构：

**System prompt（角色身份，不变）：**
```
- 角色完整描写（appearance + core_traits + psychology + stress_response）
- 说话风格 + 口癖 + 示例台词
- 背景经历（关键事件 + 对人格的影响）
- 行为规则（调用工具、保持角色一致性）
```

**User prompt（当前世界状态，每 tick 变化）：**
```
- 你现在看到的（地点 + atmosphere 感官描写 + 时间 + 天气）
- 你的身体感受（需求值→自然语言，只写有感觉的部分）
- 你现在看见了谁（附近角色 + 可观察动作）
- 你对他们的主观感觉（印象 + 未解疑惑）
- 有人对你说（信箱消息）
- 你的记忆（近期经历，含纠缠检测标注）
- 你的生活习惯（软偏好）
```

注意：**不再有"可前往的地点"章节**——这个信息通过 go_to 工具的动态参数提供。也**不再有约束警告**——不可用的工具根本不在列表里，LLM 不会尝试。只有金币不足时在身体感受中自然提及（"口袋里只剩几个硬币"）。

### 3.4 决策策略与 Token 优化

**核心原则：每个清醒的 tick 都调 LLM。没有日程兜底。**

角色的不可预测性来自于每次都是真正的"思考"，而不是执行剧本。Token 成本通过行为本身的 duration 自然控制：

- **行为 duration 即节流**：work=8 tick（2小时）、sleep=32 tick（8小时）、eat=2 tick，执行多 tick 行为期间自动跳过，不调 LLM
- **睡觉是自然的静默期**：角色自主决定何时 sleep，sleep 期间 32 tick 不消耗 LLM
- **模型分层**：日常决策用便宜模型（DeepSeek-chat），反思用稍贵的模型

**不再有的东西：**
- ~~日程兜底~~（删除。角色没有"时间表"，只有"习惯偏好"）
- ~~needsLLM 检查~~（删除。不再判断"是否需要调 LLM"，每 tick 都调）
- ~~daily_routine~~（替换为 preferences，作为 prompt 中的软提示）

**典型一天的 LLM 调用预算：**
```
sleep 8h = 32 tick 静默
工作 4h = 16 tick 静默
吃饭 3次 = 6 tick 静默
移动 5次 = 5 tick 静默
----
静默: ~59 tick
需决策: ~37 tick/天/角色
6 角色 × 37 = ~222 次 LLM 调用/天
```
比之前（~100 次/天）多出约一倍，但换来的是每次决策都真实。DeepSeek-chat 成本极低，完全可接受。

### 3.4.1 短期意图（Current Intent）

为了避免角色每个 tick 都像“重新做人”，每个角色都有一个会自然过期的 `currentIntent`：

- 别人刚刚对你说话 → `reply`
- 你刚刚主动开口 → `follow_up`
- 你刚才做某事失败 → `recover`
- 你刚到一个新地方 → `plan`

它不是外部强塞的日程，而是角色刚刚经历过的事在心里留下的余波。prompt 会把这段“你心里还挂着的事”注入进去，持续几 tick 后自然消退。

### 3.5 玩家集成

```
前端操作 → WebSocket → 转化为工具调用 → 和 AI 角色进入同一个执行管道

点击地点 → go_to(location)
输入文字 → talk(target, message)
点击物品 → give_gift(target, item) / eat(location, food)
```

玩家有自己的 CharacterState（位置、需求、金币、关系），前端显示和 AI 角色相同的状态面板。

---

## 四、记忆系统

### 4.1 短期记忆
- 滑动窗口，最近 30 条
- 包含：自己的行为、收到的消息、观察到的事件、八卦

### 4.2 信箱（Inbox）
- 别人对你说的话
- 每 tick 消费并注入 prompt 的 "## 有人对你说" 段落
- 已读消息移入短期记忆

### 4.3 长期记忆（待实现）
- SQLite + 向量搜索
- 反思机制生成高层洞察
- 跨天记忆检索

---

## 五、前端

### 5.1 布局

```
┌─────────────────────────────────────────────────┐
│  时间 | 天气 | [⏸ ½ 1x 2x 5x] | 连接状态       │
├──────────────────────┬──────────────────────────┤
│                      │  角色面板 / 全局面板      │
│   小镇地图           │  - 需求条                │
│   (地点卡片+角色)    │  - 关系                  │
│                      │  - 信箱（收到的消息）     │
│                      │  - 最近行为              │
├──────────────────────┤                          │
│  事件流（底部）       │  玩家操作区              │
│  行为/消息/八卦/事件  │  [输入框] [发送]         │
└──────────────────────┴──────────────────────────┘
```

### 5.2 玩家操作

- 点击地点 → 移动
- 选中角色 → 查看状态 / 发送消息
- 输入框 → talk
- 玩家状态面板（需求、金币、关系）

---

## 六、测试体系

- **单元测试**：时间、需求、关系、事件总线、信箱
- **集成测试**：Agent 循环 + mock LLM、信箱消息传递
- **Live 测试**：真实 LLM 调用，验证 Agent 行为
- **模拟测试**：5 角色跑 1-7 天，观察涌现行为

关键验收标准：
- 两个角色在同一地点时，通过信箱自然产生跨 tick 对话
- 角色可以"已读不回"
- 3 人在广场时可以多人互相说话
- 玩家的 talk 和 AI 的 talk 走完全相同的管道

---

## 七、行为自然度（下一阶段重点）

### 7.1 问题：行为正确但不自然

当前系统能产出**合理**的行为序列（角色按需求吃饭、工作、社交），但和 RP 系统（如 SillyTavern + 深度角色卡/预设）对比，角色行为缺少**人味**：

| 维度 | RP 系统 | 当前 Anima |
|------|---------|-----------|
| 动机 | 多层交织（好奇 + 自保 + 对过去的复杂情感） | 单一数值驱动（social < 30 → 找人说话） |
| 措辞策略 | 说"刚才那位"而非名字，用店员身份作掩护 | 直接说话，无社交策略 |
| 行为连贯性 | 犹豫 → 调整表情 → 出门 → 试探 → 退回 | 每 tick 独立决策，无跨 tick 心理线索 |
| 思考深度 | LLM 有空间推理"我为什么要这样做" | 512 token，"1-2 句想法 + 工具调用" |

**根因不是架构问题，而是决策预算和 prompt 设计问题。** 工具机制不需要改，需要改的是 LLM 在每次决策时有多少空间去"像真人一样思考"。

### 7.2 核心洞察：给 Agent 一个真实的内心世界

真实的人在做决定时，脑子里同时处理：环境感知（灯光暗了、外面下雨）、对人的印象标签（这人上次有点冷淡）、社会推理（她说"没事"但明显有事）、自我状态（我有点紧张）、历史积累（上次主动找她聊，她礼貌但有距离感）。

当前 Anima 给角色的是"报表"（`sakiko [陌生人, 亲密度:6]` + 需求数值），不是"世界"。角色看到的是数据，所以做出数据驱动的决策而非人性驱动的决策。

**核心原则：不是在外面加叙事包装，而是让角色真正"看到"更丰富的世界，它自然会做出更有人味的反应。**

对比参照：SillyTavern + 夏瑾 Pro 预设产出的对话包含环境描写、微动作、内心戏、感官细节、言行不一。其关键要素：`<thinking>` 思维链、白描基调（通过动作/语言传递情绪）、对白强化（语塞/改口/口是心非）、高 temperature (1.2)、大 token 预算 (8192)、完整聊天历史。详细分析见笔记 `~/Opensource/notes/Anima活人感分析-酒馆对比与五层改进方案.md`。

### 7.3 五层改进方案

#### 第一层：环境感知（角色看到什么）— P0

当前地点只有 `name` 和 `type`，角色不知道咖啡馆长什么样。

**改法**：给每个地点加感官描述模板，按时间段/天气动态组合。

```yaml
# data/locations/cafe.yml  新增字段
atmosphere:
  morning: |
    晨光从落地窗斜照进来，吧台后面的咖啡机发出低沉的嗡嗡声。
    空气中弥漫着现磨咖啡豆和黄油可颂的香气。只有零星几个客人。
  afternoon: |
    午后的阳光被半拉的竹帘切成条纹，投在木质地板上。
    角落的音响放着低音量的爵士乐。客人多了起来，交谈声混在一起。
  evening: |
    暖黄色的壁灯亮起来，窗外能看到港口的灯火。
    吧台上摆着今日甜品的小黑板，粉笔字有点歪歪扭扭的。
  rainy: |
    雨打在遮阳棚上，发出密集的沙沙声。
    玻璃门上蒙了一层雾气，偶尔有人推门进来，带进一股潮湿的风。
```

**Prompt 注入变化**：

```
## 你现在看到的                              ← 替代原来的 "位置: cafe (commercial)"
海风咖啡馆——午后的阳光被半拉的竹帘切成条纹，投在木质地板上。
角落的音响放着低音量的爵士乐。

在场的人：
- Maria 坐在吧台前，手里端着什么，正往你这边看    ← 替代 "maria [stranger]"
- Emily 在角落的沙发上看书，没注意到其他人
```

**实现要点**：
- `data/locations/*.yml` 每个地点加 `atmosphere` 字段（4 个时段 + 天气变体）
- `prompt-builder.ts` 的 `buildUserPrompt` 根据当前 `gameTime.timeOfDay` + `weather` 选择对应描述
- 附近角色不只给 ID + 关系，还加一句**可观察状态描述**（当前行为 + 朝向）

#### 第二层：人物印象系统（角色怎么"看"别人）— P1

当前关系只有数字（亲密度 0-100）和标签（stranger/friend）。真实的人对别人的印象是叙事性的。

**新增数据结构**：

```typescript
interface CharacterImpression {
  characterId: string;
  summary: string;        // "咖啡馆工作的女孩，礼貌但有距离感"
  observations: string[]; // 细节观察（最多保留 5 条，FIFO）
                          // "说话总先说'请容我'，像大家闺秀"
                          // "被夸时下意识否认，但表情有一瞬间动摇"
  mentalLabel: string;    // 角色心中的标签："有秘密的人"
  unresolved: string[];   // 未解疑惑（最多 3 条）
                          // "她对音乐话题很敏感，不知道为什么"
  lastUpdated: number;    // tick
}
```

**生成方式**：每次两个角色互动结束后，加一个**反思步骤**（轻量 LLM 调用）：

```
你刚才和丰川祥子进行了以下互动：
[完整对话记录]

请更新你对她的印象：
1. 一句话概括她给你的整体印象
2. 这次互动中你注意到的细节（动作、语气、反应）
3. 你现在给她的"标签"
4. 有什么疑惑或好奇的地方
```

**Prompt 注入变化**：

```
## 你对在场的人的印象                        ← 替代 "附近: sakiko [acquaintance, 亲密度:24]"
丰川祥子——咖啡馆工作的女孩。很有礼貌，但总是客气地保持距离。
你注意到：她说"请容我"，像是从小受过很好的教育；她的手指很修长，像弹乐器的人。
你的疑惑：她为什么来这个小镇？她好像在躲什么。
```

**存储**：每个角色的 CharacterState 新增 `impressions: Map<string, CharacterImpression>`。短期可存内存，长期迁入 SQLite。

#### 第三层：内心独白分级（角色怎么"想"）— P0

当前所有场景统一 512 token + "先简短说说你的想法（1-2句）"，思考空间极度压缩。

**按场景分级**：

| 场景判断条件 | maxTokens | 思考指令 |
|---|---|---|
| 独处 + 日常行为 | 512 | "简短说说你的想法（1-2句），然后调工具" |
| 附近有人 / 信箱有消息 | 1024 | "详细说说：你看到了什么、感受到什么、对在场的人有什么想法、打算怎么做、为什么" |
| 对话模式中（见第四层） | 1536 | 段落级内心分析 + 叙事输出 |

**社交引导 prompt**（注入 system prompt 的行为规则段）：

```
## 社交行为指引
- 说话时考虑你和对方的关系、你当前的心情、以及你想达到什么目的
- 你可以选择说什么、不说什么、怎么说——不是每次都要直来直去
- 你可以犹豫、改口、词不达意、言不由衷——这些都是真实的表达
- 如果对方让你不舒服，你可以转移话题、找借口离开、或者沉默不语
- 根据当前位置/附近角色，选择性回忆相关的过去经历
```

**实现要点**：
- `agent-loop.ts` 根据 `nearbyCharacters.length > 0 || inboxMessages.length > 0` 判断场景级别
- `prompt-builder.ts` 根据场景级别切换思考指令模板
- 内心独白的完整文本存入 `MemoryEntry.content`（type: "thought"），影响后续决策

#### 第四层：对话模式切换 — P0

这是最关键的改造。当前每个 talk 都是独立的 tool call，角色之间没有"对话流"，导致多轮对话缺乏递进感、内容重复。

**触发条件**：两个角色在同一地点，且在 2 个 tick 内互相 talk 了 2 次以上。

**对话模式流程**：

```
1. 检测到对话触发 → 暂停双方的普通 tick 循环
2. 构建对话 prompt：
   - 角色身份（system prompt 不变）
   - 环境描写（第一层）
   - 对方的印象（第二层，如有）
   - 角色当前内心状态（今天发生了什么、在想什么）
   - 完整对话历史（本次对话的所有消息，非摘要）
3. 每轮生成要求：
   - 内心活动（不出声的想法）
   - 身体语言和微表情
   - 说的话（或选择不说话）
   - 观察到的对方反应
4. 创作风格指引（借鉴夏瑾预设关键要素）：
   - 白描手法，通过动作和语言本身传递情绪，不解释
   - 角色可以犹豫、改口、词不达意、言不由衷
   - 包含环境细节和感官描写
5. 对话结束条件：一方选择离开/沉默/话题自然终结/达到轮次上限
6. 结束后：双方各做一次反思更新印象（第二层）
7. 恢复普通 tick 循环
```

**与第一性原理的兼容**：
- 每轮仍是独立的 LLM 调用，角色仍可选择不回应
- 不引入 Conversation 对象，对话状态只是临时的上下文窗口
- 其他角色在对话期间继续正常 tick（世界不暂停）
- 第三人可以走过来加入（检测到新角色在场 → 扩展对话上下文）

**输出格式**：对话模式的 LLM 输出不再是纯 tool call，而是结构化叙事：

```json
{
  "innerThought": "Maria又来了。她每次都这么热情...",
  "action": "祥子的手指在围裙上搓了搓，那是清洁剂留下的干涩感。",
  "dialogue": "...麻烦您了。",
  "observation": "Maria的笑容像是什么都没发现一样自然，但她眼角的弧度变了。",
  "continueConversation": true
}
```

前端可以将这些字段渲染为富文本事件流，而非单纯的聊天气泡。

#### 第五层：观察与社会推理（角色怎么理解别人的行为）— P2

角色在场时不只"看到"别人，还会**解读**别人的行为。

**触发条件**：角色 A 在某地点停留 2+ tick，且该地点有其他角色正在执行可观察行为。

**实现**：在 tick 结束时，对在场角色做一个轻量级观察生成：

```
你在咖啡馆工作时，注意到：
- Emily 在角落看了两小时的书，期间看了三次手机又放下
- 她点的咖啡一直没喝，已经凉了

你觉得她怎么了？（一句话）
```

→ Sakiko: "她好像在等什么人。"

这条观察进入 Sakiko 的记忆（type: "observation"），下次见到 Emily 时影响行为——也许默默换一杯热咖啡。

**成本控制**：
- 每 tick 最多为每个角色做 1 次观察（附近有 2+ 人且自己处于空闲状态时）
- 使用最便宜的模型，maxTokens 128
- 观察不是每 tick 都做，可设为每 4-8 tick 一次

### 7.4 实施优先级

| 阶段 | 内容 | 改动范围 | 预期效果 |
|---|---|---|---|
| **P0-a** | 环境感知 + 内心独白分级 | 数据文件 + prompt-builder | 行为有场景感，社交思考更丰富 |
| **P0-b** | 对话模式 | 新模块 conversation-mode.ts + agent-loop 改动 | 对话质量质变 |
| **P1** | 人物印象系统 | 新数据结构 + 反思步骤 + prompt 注入 | 关系发展有故事感 |
| **P2** | 观察与社会推理 | 新观察步骤 + 记忆类型 | 涌现不言而喻的默契 |

### 7.5 验收标准

**P0 验收**：
- 角色在咖啡馆/海边/花店的行为描述体现出环境感知（提及光线/声音/气味）
- 社交场景的 thought 字段展现多层动机（不只是"social 低，找人说话"）
- 两个角色进入对话模式后，生成的内容包含微动作、内心戏、环境细节
- 对话模式中角色能犹豫、改口、言不由衷
- 对话模式中不再出现重复自我介绍

**P1 验收**：
- 角色对同一个人的第 1 次和第 5 次见面，说话方式有明显差异
- 印象系统中出现角色自发生成的标签和未解疑惑
- 角色基于印象中的疑惑主动探寻（如试探性提起音乐话题）

**P2 验收**：
- 角色观察到他人异常行为后，记忆中出现解读
- 基于观察做出未经沟通的关怀行为（默默换热咖啡、主动让座）
